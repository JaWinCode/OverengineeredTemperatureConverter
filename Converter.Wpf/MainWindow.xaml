<Window x:Class="Converter.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converter.Wpf"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <Border Height="30" BorderThickness="0 0 0 1" BorderBrush="#D1D1D1">
            <Grid Background="#F9F8F9" MouseDown="TopBar_DragMove">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock 
                    Grid.Column="1" 
                    Text="Converter" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    FontWeight="Bold"
                    />
            </Grid>
        </Border>
        <Grid Height="770">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border BorderThickness="0 0 1 0" BorderBrush="#D1D1D1">
                <StackPanel Background="#F9F8F9">
                    <RadioButton 
                        Content="Temperaturen"
                        Margin="0,20,0,0"
                        Height="40"
                        BorderThickness="0"
                        Style="{StaticResource NavButton}"
                        IsChecked="True"/>

                    <RadioButton 
                        Content="Nummersysteme"
                        Height="40"
                        BorderThickness="0"
                        Style="{StaticResource NavButton}">
                    </RadioButton>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</Window>
