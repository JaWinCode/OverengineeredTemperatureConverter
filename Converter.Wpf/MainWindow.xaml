<Window x:Class="Converter.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converter.Wpf"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Title="Converter" Height="450" Width="800">
    <Border
        BorderBrush="#D1D1D1"
        BorderThickness="1"
        CornerRadius="15">
        <StackPanel>
            <Border CornerRadius="15,15,0,0" Height="30" BorderThickness="0 0 0 1" BorderBrush="#D1D1D1" Background="#F9F8F9" MouseDown="TopBar_DragMove">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock 
                        Grid.Column="1" 
                        Text="Converter" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        FontWeight="Bold"/>
                    <Button Background="Transparent" Grid.Column="2" Click="Close_Window" BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="LightGray"/>
                                <Setter Property="BorderBrush" Value="Gray"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <Image
                            Source="{StaticResource CloseImage}"
                            VerticalAlignment="Center" 
                            HorizontalAlignment="Center"
                            RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Button>

                </Grid>
            </Border>
            <Grid Height="420">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Border 
                    BorderThickness="0, 0, 1, 0" 
                    BorderBrush="#D1D1D1" 
                    Background="#F9F8F9"
                    CornerRadius="0,0,0,15">
                    <StackPanel>
                        <RadioButton 
                            Content="Temperaturen"
                            FontSize="14"
                            Margin="0,20,0,0"
                            Height="40"
                            BorderThickness="0"
                            Style="{StaticResource NavButton}"
                            IsChecked="True"/>

                        <RadioButton 
                            Content="Nummersysteme"
                            FontSize="14"
                            Height="40"
                            BorderThickness="0"
                            Style="{StaticResource NavButton}"/>
                    </StackPanel>
                </Border>
                <Border
                    Grid.Column="1"
                    Background="White"
                    CornerRadius="0,0,15,0">
                    <ListView Name="listView">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Unit" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                <GridViewColumn Header="Result" DisplayMemberBinding="{Binding Age}" Width="100"/>
                                <GridViewColumn Header="Suffix" DisplayMemberBinding="{Binding Country}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Border>
            </Grid>
        </StackPanel>
    </Border>
</Window>
